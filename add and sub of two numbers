#include <stdio.h>
int main(){
int a,b,add,sub;
printf("Enter two number");
scanf("%d%d",&a,&b);
add=a+b; 
sub=a-b;
printf("addition of two numbers %d\n",add);
printf("subraction of two numbers %d\n",sub);
return 0;
}
