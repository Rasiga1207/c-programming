#include <stdio.h>
int main(){
int a,b,mul,div;
printf("Enter two number");
scanf("%d%d",&a,&b);
mul=a*b; 
div=a/b;
printf("multiplication of two numbers %d\n",mul);
printf("division of two numbers %d\n",div);
return 0;
}
